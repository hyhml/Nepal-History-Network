# 尼泊尔人物—组织时序图设计规范

版本：0.2.1。此文件记录已经采用的设计规则。新要求的记录见 `requests.jsonl`，落实新要求时同步修改此规范。

## 范围与资料

- 当前案例呈现阿迪卡里和腊伊玛吉，重点时段为1954—2012年。人物、组织、任职、事件、组织关系、阶段和政治背景分别存放在 CSV 表中，以稳定 ID 关联。
- 图中记录以汪亭友书内第28—32页为当前依据；事实表保留 `source_id` 与来源目录，但目前不在图上显示来源。只有用户明确要求时才改变这一展示规则。
- 公开示例和本地数据都保存当前事件对应的书中摘录；人物事件与政治背景节点的悬浮说明优先显示摘录。读入其他资料前先在 `materials/catalog.json` 中登记。
- 年份、月份和日期要保留实际精度。只知道年份时，CSV 的 `YYYY-01-01` 是制图占位；说明文字不得把它表述为确切的1月1日。推定任期用虚线和说明标出。

## 图形结构

- 横向是固定组织列，纵向是自上而下的时间。最左列用于书中明确记载的政治背景；任职、公职和党派活动使用对应组织列。
- 人物共用组织列。`x_offset` 只区分同一人物的党职和公职等不同角色，不按人物数量划分固定子轨。
- 人物使用稳定颜色与标记形状。任职为纵向线段，跨组织活动可用连线；组织分裂、合并和正式另立用紫色虚线表示。
- “腊伊玛吉控制的中央”“尼共（腊伊玛吉）”“再次建党”“尼泊尔人民党”在同一组织大列内以阶段名称和横向虚线区分。

## 交互

- 默认人物与关联组织信息淡显；悬停人物节点或任职线时，其全部事件、任职、组织关系和阶段标注同步高亮，其他人物淡化。
- 单击锁定聚焦；再次单击取消。Shift＋单击或右侧比较模式可同时聚焦最多3人。人物搜索、全部人物按钮和 Esc 用于定位或清除。
- 右侧固定显示快捷操作说明。鼠标悬浮事件节点展示该事件的书中摘录；摘录缺失时才展示简要描述。
- `related_person_ids` 用分号关联一人或多人。聚焦其中任何一人时，该组织分合线或阶段标注高亮。未建人物轨迹的对方组织不自动推定为某位人物。
- 高亮使用一次批量更新人物与组织关系线；阶段线和名称的透明度随同一聚焦状态更新，避免逐条重绘。

## 当前案例的关键标注

- 阿迪卡里1954年任总书记；1957年缺席尼共二大。书中写明他1961年因反对无党派评议会制度入狱9年，未注明入狱月份。
- 腊伊玛吉的1954年节点承载其出生、早年赴印、1947—1948年组织活动和1954年进入尼共中央的叙述；终点为2012年去世。
- 原书未给出部分王室或政府职务的精确任期，应用事件点或虚线呈现，不补造具体日期。

## 项目边界与版本

- 图表输入和输出路径必须在项目根目录内。公开仓库只追踪代码、规范、目录和可公开的结构化示例。
- `materials/local/` 存放原始 PDF 与本地数据副本，`output/` 存放可重建的 HTML，两者均不提交到 Git。事件摘录存放在可版本化的示例 CSV 中。
- 公开交互图由 `examples/raimajhi-life/` 构建到 `docs/index.html`，通过 GitHub Pages 供任何人访问；图中展示书中摘录并从 Plotly CDN 加载脚本。公开版和本地离线版使用同一个绘图程序，本地版将脚本嵌入 HTML。
- 每次需求变更保留原话并更新此规范；可审查的修改进入 Git 提交，里程碑由 `VERSION`、`CHANGELOG.md` 和 Git 标签共同记录。
